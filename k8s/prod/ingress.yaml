apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chat-app-ingress
  namespace: chat-prod
  labels:
    app: chat-app
    env: prod
spec:
  ingressClassName: nginx
  rules:
  - host: chat-prod.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: chat-app-service
            port:
              number: 80
